<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Ticketac</title>
</head>

<body>

    <%- include('navbar') %>

        <div class="home">
            <h3 class="homeTitle">Tickets available : <%=dateFormat(availableJourneys[0].date)%>
            </h3>

            <div class="resultField">

                <% for (let i=0; i<availableJourneys.length; i++){ %>

                    <div class="card w-50">
                        <div class="card-body resultCard">
                            <div>
                                <%=availableJourneys[i].departure%> / <%=availableJourneys[i].arrival%>
                            </div>
                            <div><small class="text-muted">train departure at: <%=availableJourneys[i].departureTime%>
                                </small>
                            </div>
                            <div class="d-flex justify-content-between align-items-center">
                                <div style="font-size: 20px;">
                                    <%=availableJourneys[i].price%>€
                                </div>
                                <a href="/checkout?id=<%=availableJourneys[i]._id%>"><button type="button"
                                        class="btn btn-danger mx-sm-3">OK</button></a>
                            </div>
                        </div>
                    </div>
                    <%}%>
            </div>




            <%- include('footer') %>
        </div>

</body>

</html>